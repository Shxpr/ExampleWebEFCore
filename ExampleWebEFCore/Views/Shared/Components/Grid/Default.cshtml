@model GridViewModel
<table class="@Model.StyleHeader">
    <thead>
        <tr>
            @foreach (var item in Model.Columns)
            {
                <th scope="col">@item</th> 
            }
        </tr>
    </thead>
    <tbody>    
    </tbody>
</table>